p There are {{posts.length}} posts
div(ng-repeat='post in posts')
  h3 {{post.title}}
  div {{post.text}}
  a(href='/postRead-server-view/{{post.id}}') More
  |  - 
  a(href='/postEdit-server-view/{{post.id}}') Edit
  |  - 
  a(href='/postDelete-server-view/{{post.id}}') Delete
